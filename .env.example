# --- Neon Auth (Stack Auth) -- from Neon dashboard > Auth, or app.stack-auth.com ---
NEXT_PUBLIC_STACK_PROJECT_ID=
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=
STACK_SECRET_SERVER_KEY=

# --- Database ---
# Postgres role `cassets_portal`: SELECT-only on cassets.v_portal_* views and
# cassets.investor_documents / cassets.investor_users, plus INSERT on
# cassets.portal_access_log.
PORTAL_DATABASE_URL=
